{
  "name": "nanoplayer-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "happy-dom": "^20.11.1",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
